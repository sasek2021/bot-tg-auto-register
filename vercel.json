{
  "version": 2,
  "builds": [
    {
      "src": "proxy_server.py",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "python3 -m venv venv"
      }
    },
    {
      "src": "bot.py",
      "use": "@vercel/python",
      "config": {
        "buildCommand": "pip install -r requirements.txt"

      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "proxy_server.py"
    },
    {
      "src": "/(.*)",
      "dest": "bot.py"
    }
  ]
}
